# 证明一元线性回归中，残差之和必然等于0

样本拟合模型：
$$
\hat{Y_i}=\hat{β_0}+\hat{β_1}X_i
$$


损失函数RSS：
$$
RSS=\sum_{i=1}^n e_i^2=\sum_{i=1}^n(Y_i-\hat{Y}_i)^2
$$
对斜率\(\hat\beta_1\)求偏导，令导数等于0：
$$
\frac{\partial RSS}{\partial \hat\beta_1} = \sum 2\big(Y_i - \hat\beta_0 - \hat\beta_1 X_i\big)(-X_i) = 0\\
\sum X_i\big(Y_i - \hat\beta_0 - \hat\beta_1 X_i\big) = 0
$$


对截距\(\hat\beta_0\)求偏导，令导数等于0：
$$
\frac{\partial RSS}{\partial \hat\beta_0} = \sum 2\big(Y_i - \hat\beta_0 - \hat\beta_1 X_i\big)(-1) = 0\\
\hat{β_0}=\bar{Y}-\hat{β_1}\bar{X}
$$
残差和：
$$
\sum_{i=1}^n e=\sum_{i=1}^n (Y_i-\hat{Y_i})=\sum_{i=1}^n [Y_i-\hat\beta_0 - \hat\beta_1 X_i]\\
\sum e_i=\sum Y_i-n\hat{\beta_0}-\hat{\beta_1}\sum X_i=0
$$

# 证明残差与自变量乘积之和必然等于0

$$
\sum_{i=1}^n x_ie_i=\sum_{i=1}^n x_i(Y_i-\hat\beta_0-\hat\beta_1 X_i)
$$

由对斜率\(\hat\beta_1\)求偏导可得到

\(\sum X_i\big(Y_i - \hat\beta_0 - \hat\beta_1 X_i\big) = 0\)

即\(\sum_{i=1}^n x_ie_i=0\)



# 为什么多元回归中，在一个模型中添加一个纯随机生成的无关特征，\(R^2\)会上升或保持不变，而\(R^2_{adj}\)会下降？

$$
R^2=1-\frac{RSS}{TSS}\\
R^2_{adj}=1-\frac{RSS/(n-p-1))}{TSS/(n-1)}
$$

添加无关特征时，OLS会强行拟合噪音，RSS产生小幅度下降，而TSS不变，整体下降，导致\(R^2\)上升或保持不变。

添加无关特征时，变量增加，p增大，自由度n-p-1减小，最终RSS/(n-p-1)增大，\(R^2_{adj}\)变小。
